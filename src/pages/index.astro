---
import Layout from '../layouts/Layout.astro';
import IntroCard from '../components/IntroCard.astro';
import ContactsCard from '../components/ContactsCard.astro';
import ProjectsCard from '../components/ProjectsCard.astro';
import TechnologiesCard from '../components/TechnologiesCard.astro';
import TimeZone from '../components/TimeZoneCard.astro';
import AboutMe from '../components/AboutMe.astro';
import Now from '../components/Now.astro';
---

<script>
  import { stagger, spring, timeline, type TimelineDefinition } from 'motion';
  import { loaderAnimation } from '../lib/constants';
  const cards = document.querySelectorAll('.card');

  const sequence = [
    loaderAnimation,
    [
      cards,
      { y: ['40%', '0%'], opacity: [0, 1] },
      {
        at: '-0.1',
        duration: 0.4,
        delay: stagger(0.3),
        easing: spring({ velocity: 100, stiffness: 50, damping: 10 }),
      },
    ],
  ];

  timeline(sequence as TimelineDefinition);
</script>

<Layout
  title="Nicolás Tonelli - Front-End Developer"
  description="I'm a developer based in Argentina, passionate about user experience and specializing in improving apps performance and micro-interactions. My primary tools of choice include: JavaScript, React, Redux, Typescript, Angular, and Node.js."
>
  <main
    class="text-white m-auto p-2 grid gap-2 max-w-6xl overflow-hidden relative w-full sm:p-4 sm:gap-2 md:grid-cols-2 md:gap-3 md:p-6 lg:h-screen lg:grid-rows-8 lg:grid-cols-4 lg:gap-4 lg:max-h-[800px]"
  >
    <IntroCard />
    <AboutMe />
    <ContactsCard />
    <ProjectsCard />
    <Now />
    <TimeZone />
    <TechnologiesCard />
  </main>
</Layout>
